class Solution:
    def isPrefixString(self, s: str, words: List[str]) -> bool:
        result = ''
        for i in range(len(words)):
            result += words[i]
            if result == s:
                return True
        return False
